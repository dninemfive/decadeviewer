<Window x:Class="DecadeViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DecadeViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Name="ButtonHolder" Panel.ZIndex="1" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <TextBox Name="FolderEntry" Grid.Row="0" Text="C:/Users/dninemfive/Music/Files/" HorizontalAlignment="Center"/>
            <Button Click="Button_Profile" Grid.Row="1" Content="Click" MaxWidth="100"/>
            <ComboBox Name="WeightDropdown" Grid.Row="2" MaxWidth="100"/>
        </Grid>
        <ScrollViewer>
            <ListView Name="DecadeList">
                
            </ListView>
        </ScrollViewer>
    </Grid>
</Window>
